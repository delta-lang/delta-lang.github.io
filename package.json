{
  "name": "delta-sandbox",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node server/server.js",
    "preinstall": "[ \"$NODE_ENV\" != production ] && exit 0; server/install_delta.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "emlai",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.17.0",
    "cors": "^2.8.1",
    "express": "^4.15.0",
    "os-tmpdir": "^1.0.2"
  }
}
