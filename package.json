{
  "name": "cx-online-compiler",
  "version": "1.0.0",
  "description": "",
  "repository": "https://github.com/cx-language/cx-language.github.io",
  "scripts": {
    "format": "prettier --write ./**/*.js ./**/*.css",
    "start": "node server/server.js",
    "preinstall": "[ \"$NODE_ENV\" != production ] && exit 0; server/install-cx.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "emlai",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "os-tmpdir": "^1.0.2"
  },
  "devDependencies": {
    "prettier": "1.18.2"
  },
  "prettier": {
    "printWidth": 100,
    "tabWidth": 4
  }
}
